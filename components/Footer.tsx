const Footer = () => {
  return (
    <footer className="row-start-3 flex gap-[24px] flex-wrap items-center justify-center bg-primary">
      this is the footer
    </footer>
  );
};

export default Footer;
